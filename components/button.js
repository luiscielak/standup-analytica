import { Button } from 'react-native';

export default class Button extends React.Component {
  render() {
    return (
      <View>
        <Button
          title="Stand Up!"
          color="#FF9500"
        />
      </View>
    );
  }
}
